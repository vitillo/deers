# DEERS Backlog

- Add operations missing to implement nanochat GPT model
- Add tests to each file and clean it up (done so far: tensor, parameter, storage)
- Make it easy to extend storage with new operators! Easy to add new ops today but not easy to add new kernels... how can we fix that. And actually check how I would extend tensor end to end to add something new, not clear how to use ops from another libray. Maybe for now OK to focus on making it easy just from within the library?
- Build project up to run GPT + Reinforcement learning + pi support, use nano chat as guiding post
